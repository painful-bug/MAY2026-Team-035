// Invites are minted at runtime by the admin "Add Resident" flow, so this seeds
// empty. Shape: { id, token, apartmentId, phones, createdAt, expiresAt, used }.
export const initialInvitations = [];
